i have crate a database named foodcarting , so for now i have created menu table to store menu details , and user table to store users here are the table schemas menu:CREATE TABLE menu(
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(20) UNIQUE,
  category VARCHAR(10),
  price INT,
  discount INT,
  descrip VARCHAR(40),
  image VARCHAR(100),
  badge VARCHAR(10),
  veg TINYINT(1),
  available TINYINT(1) DEFAULT 1
);

here is user table: users
Columns:
id int UN AI PK 
username varchar(50) 
password varchar(255) 
email varchar(255) 
created_at timestamp 
updated_at timestamp

i need a order history table to be created 

i want timestap when the order placed date with time

tell me a way that i dont need to manually enter created_at and updated_at it should be automatic
you have my app details , you can analyze and tell me the imporvements 
cart will be temporary may be stored in session for 24 hours when order placed then cart will be empty also session data of cart will be erased .  

for now write all those table 
you need to write api so that orderplaced will go to orderhistory data base 
another thing order history will be individual means user will be included in them user id will be there wil order.

write all the api and middleware that i will  be need to ensure communication with backend .
for now do this next what can be imporved or add tell me after my approval do it 

